import { FoodiesDirective } from './foodies.directive';

describe('FoodiesDirective', () => {
  it('should create an instance', () => {
    const directive = new FoodiesDirective();
    expect(directive).toBeTruthy();
  });
});
